# Upcoming Chain Projects Streamlit App

Single-page Streamlit overview for upcoming chain projects.

## Run Locally

```powershell
python -m pip install -r requirements.txt
streamlit run streamlit_app.py
```

## Deploy

Use `streamlit_app.py` as the Streamlit Community Cloud entrypoint.
